import type { PageServerLoad } from "./$types";

export const load: PageServerLoad = async ({ fetch }) => {
  const response = await fetch("http://localhost:3000/books/page");
  const data: BookPage = await response.json();

  console.log(data);

  return {
    items: data,
  };
};
